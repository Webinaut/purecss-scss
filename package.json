{
  "name": "@webinaut/purecss-scss",
  "version": "1.0.1",
  "description": "Pure CSS framework, converted to Sass and ready to use in Sass powered applications",
  "keywords": [
    "pure",
    "purecss",
    "scss",
    "sass"
  ],
  "main": "dist/pure.css",
  "scss": "src/pure.scss",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "sass src/pure.scss dist/pure.css --style compressed",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "modern-normalize": "^3.0.1"
  },
  "devDependencies": {
    "sass": "^1.99.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
